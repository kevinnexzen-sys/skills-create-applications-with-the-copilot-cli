# This workflow has been disabled - Not applicable for this Node.js project
# Original: Deno runtime configuration
# Status: DISABLED

name: "[DISABLED] Deno"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "This workflow is not needed for this project"
